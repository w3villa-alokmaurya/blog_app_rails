<div>
    <a href="<%=@Article.slug%>/edit">Edit Articles</a>
    <h1> <%= @Article.title %> </h1> 

    <img src="<%= url_for(@Article.featured_image) %>" alt="">
    <p><%= @Article.category_id %></p>
    <p><b><%= @Article.user_id %></b></p>
    <p> <%= @Article.body %> </p>
    <p><%= @Article.tags %></p>
</div>